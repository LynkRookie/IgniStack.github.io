/**
 * main.jsx
 * 
 * En un proyecto Vite puro, este sería el punto de entrada:
 * 
 *   import React from "react"
 *   import ReactDOM from "react-dom/client"
 *   import App from "./App"
 *   import "./styles/global.css"
 *
 *   ReactDOM.createRoot(document.getElementById("root")).render(
 *     <React.StrictMode>
 *       <App />
 *     </React.StrictMode>
 *   )
 *
 * En este proyecto Next.js, el punto de entrada es app/page.tsx
 * que importa el componente App desde src/App.jsx.
 */

export { default } from "./App"
